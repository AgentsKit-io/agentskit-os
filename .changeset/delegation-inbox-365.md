---
'@agentskit/os-dev-orchestrator': patch
---

#365: dispatch delegation conflicts/failures/missing-artifacts to a HITL inbox via new `hitlInbox` option, and validate `expectedArtifacts` per shard. Adds `missingArtifacts` to subtask rows and `hitlTaskId` to the report.
