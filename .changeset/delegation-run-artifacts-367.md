---
"@agentskit/os-dev-orchestrator": minor
"@agentskit/os-cli": minor
---

Add delegation coding-run artifact JSON (#367): `runDelegatedCodingTask` accepts `artifacts` like the benchmark, shared `collectGitHeadDiffSnapshot` and `artifactFilenameForDelegationStep`, and `coding-agent delegate` gains `--capture-run-artifacts` / `--artifact-trace-id` mirroring `coding-agent benchmark`.
