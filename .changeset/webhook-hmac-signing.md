---
"@agentskit/os-core": patch
"@agentskit/os-headless": patch
---

Add webhook request signing/verification (HMAC SHA-256) and a headless webhook server helper to verify inbound webhook triggers before running flows.

