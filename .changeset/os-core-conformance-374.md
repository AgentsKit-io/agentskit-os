---
'@agentskit/os-core': patch
---

Expand coding-agent `runConformance` (#374): scenario probes (no-diff, failing tests, permission denied, artifacts, timeout, fast budget), optional `cancelTask`, `marketplaceBadge` on reports, and `CONFORMANCE_PROMPTS` for adapters. Document contract in `packages/os-core/README.md`.
