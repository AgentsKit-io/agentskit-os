---
'@agentskit/os-flow': patch
---

#61: add `FLOW_TEMPLATE_SEEDS` + `findFlowTemplateSeed` — visual-editor-ready FlowConfig seeds (`blank-agent`, `compare-2`, `trigger-to-tool`) the `agentskit-os flow new` command scaffolds. Pure; each seed parses through `parseFlowConfig` so the visual editor opens it round-trip.
