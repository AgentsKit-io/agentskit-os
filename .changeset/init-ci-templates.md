---
"@agentskit/os-cli": minor
---

Add `agentskit-os init-ci` command that scaffolds GitHub Actions templates (`agentskit-ci.yml`, `agentskit-evals.yml`, `agentskit-deploy.yml`) into a project's `.github/workflows/` directory.
