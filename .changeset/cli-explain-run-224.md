---
'@agentskit/os-cli': patch
---

#224: add `agentskit-os explain --artifact-dir <path> [--run-id <id>] [--json]` — walks the persisted `coding-run-artifact-*.json` bundles produced by `--capture-run-artifacts` (#367) and renders a plain-English step list (or JSON) of what each provider did.
