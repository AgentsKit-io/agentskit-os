---
"@agentskit/os-core": minor
---

Add `WorkspaceConfig` Zod schema (M1, first contract). Exposes `WorkspaceConfig`, `WorkspaceIsolation`, `parseWorkspaceConfig`, `safeParseWorkspaceConfig`, and `SCHEMA_VERSION`. Zod is a `peerDependency` per ADR-0004.
