---
'@agentskit/os-core': patch
---

#123: add `entitlementsFor` + `evaluatePlanUsage` — pure plan-tier entitlement table (free / pro / team / enterprise / self-hosted) and verdict that compares observed `PlanUsage` against the plan caps with per-key reasons.
