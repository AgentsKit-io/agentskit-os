---
"@agentskit/os-core": minor
---

Add `AgentConfig` Zod schema and shared `_primitives` module (`Slug`, `Tag`, `TagList`, `VaultSecretRef`). Workspace schema refactored to consume primitives. New subpath export `@agentskit/os-core/schema/agent`.
