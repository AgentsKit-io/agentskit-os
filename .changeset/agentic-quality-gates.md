---
"@agentskit/os-cli": patch
"@agentskit/os-dev-orchestrator": patch
---

Add a configurable `--timeout-ms` benchmark option so CLI benchmark tests and agent dogfooding runs do not depend on long-running local provider defaults.
