---
'@agentskit/os-dev-orchestrator': patch
---

Extend #376 failure taxonomy integration: include failure codes in delegation trace nodes and add `buildCodingFailureIncidentRecord()` for traces/artifacts and later incident conversion.

