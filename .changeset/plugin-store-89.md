---
'@agentskit/os-marketplace-sdk': patch
---

#89: add `PluginStoreEntry` (verification level + provenance + audits) plus `filterPluginStoreEntries`, `latestAudit`, and `renderStoreBadge`. Pure helpers the third-party plugin store renders for verified-badge gating.
