# Changesets

Run `pnpm changeset` whenever a PR causes a user-visible change. Pick the appropriate semver bump and write a one-line summary of the impact. CI fails if package source changes lack a changeset.

See https://github.com/changesets/changesets for more.
