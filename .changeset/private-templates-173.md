---
'@agentskit/os-marketplace-sdk': patch
---

#173: add `filterForPrivateViewer` + `tagPrivateScope` — pure scoping helpers for a private team template library. Public listings always pass; `private` listings only appear when the viewer is a member of the matching `team:`/`org:`/`workspace:` tag.
