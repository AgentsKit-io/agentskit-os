---
'@agentskit/os-core': patch
---

#99: add `cloneAgentWithVariation` — extends `forkAgentConfig` with a structured `CloneVariation` (model override, clamped temperature delta, system-prompt suffix, tool add/remove, variant tag). Pure; persistence stays a caller concern.
