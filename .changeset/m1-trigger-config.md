---
"@agentskit/os-core": minor
---

Add `TriggerConfig` discriminated union covering eight kinds: `cron`, `webhook`, `file`, `email`, `slack`, `github`, `linear`, `cdc`. New subpath export `@agentskit/os-core/schema/trigger`.
