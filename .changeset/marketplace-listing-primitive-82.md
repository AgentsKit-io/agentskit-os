---
'@agentskit/os-marketplace-sdk': patch
---

#82: add `MarketplaceListing` shape + `searchListings` (keyword/category/tag filter, rank/rating/installs/recency sort) + `evaluateInstall` (integrity + pinned-version verdict). Pure helpers the desktop browser will render and the 1-click install flow runs before extracting a bundle.
