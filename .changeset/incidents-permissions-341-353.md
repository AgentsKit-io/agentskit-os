---
'@agentskit/os-dev-orchestrator': minor
'@agentskit/os-observability': minor
---

Agent incident model (#341): create, transition (mitigate, rollback, RCA,
resolve, postmortem, close), audit-export summary, and markdown rendering in
`@agentskit/os-observability`.

Coding permission profiles (#353): preset profiles, glob-based
`evaluateCodingPermission`, and audit event builder in
`@agentskit/os-dev-orchestrator`.
