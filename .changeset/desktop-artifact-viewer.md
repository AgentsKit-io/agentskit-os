---
"@agentskit/desktop": patch
---

U-7 immersive artifact viewer. Detects artifact kind by MIME + content (code, JSON, YAML, CSV, SVG, Mermaid, HTML, Markdown, image). One renderer per kind. Fullscreen mode via Cmd+Shift+A. HTML sandboxed iframe (`sandbox="allow-scripts"` only). Closes #119
