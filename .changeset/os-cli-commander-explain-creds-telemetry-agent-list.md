---
'@agentskit/os-cli': patch
---

Migrate `config explain`, `creds`, `telemetry`, and `agent list` to Commander with `runCommander`; align config explain and subcommand tests with Commander help and error wording.
