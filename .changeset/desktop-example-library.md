---
"@agentskit/desktop": patch
---

Example library indexed by intent. 12 examples linked to os-templates. Sidebar entry, search + intent filters, "Try in OS" via sidecar `templates.scaffoldFrom` (TODO #91). Closes #180
