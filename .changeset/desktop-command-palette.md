---
"@agentskit/desktop": patch
---

apps/desktop — D-6 global command palette (Cmd/Ctrl+K). Built-in commands cover navigation, runtime control (pause/resume), theme toggle, event-feed clear. Pluggable via `registerCommand` in the provider. Closes #40
