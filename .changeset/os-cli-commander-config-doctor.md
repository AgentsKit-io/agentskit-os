---
'@agentskit/os-cli': patch
---

Migrate `config validate`, `config migrate`, `config diff`, and `doctor` to Commander with `runCommander`; align help and excess-argument tests with Commander output.
