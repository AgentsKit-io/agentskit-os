---
"@agentskit/desktop": patch
---

D-11 global fuzzy search overlay (Cmd/Ctrl+/). Searches workspaces, agents, flows, runs, traces, palette commands, docs. Pure scoring (subsequence + prefix bonus). Find-similar button stub references sidecar `search.findSimilar` (TODO #91). Closes #45
