---
'@agentskit/os-cli': patch
'@agentskit/os-dev-orchestrator': patch
---

#368: add `--print-report` to the `coding-agent benchmark` and `coding-agent delegate` CLIs to render the markdown task report on stdout without `--artifact-dir`. Aggregate now includes a `benchmarkScore` (mean completeness across providers).
