#!/usr/bin/env sh

# Fast checks on every commit.
# Heavier (build + public-API + typecheck) run in pre-push.
pnpm check:quality-gates && \
  pnpm check:adr-0002 && \
  pnpm check:adr-0004 && \
  pnpm check:adr-0014
