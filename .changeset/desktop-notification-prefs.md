---
"@agentskit/desktop": patch
---

Notification preferences: per-event routing (panel / os-toast / desktop-alert / silent) + quiet hours with allow-critical override. Persisted; pure routing engine; UI modal with routing matrix + time pickers. Closes #246
