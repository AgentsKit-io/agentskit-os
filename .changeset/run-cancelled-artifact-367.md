---
'@agentskit/os-dev-orchestrator': patch
---

#367: persist a `run_cancelled` artifact when `runCodingAgentBenchmark` aborts mid-run, with a dedicated filename suffix. Adds `run_cancelled` to `CodingRunArtifactPhase`.
