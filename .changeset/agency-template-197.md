---
'@agentskit/os-templates': patch
---

#197: add `agencyContentApprovalTemplate` — agency-client content approval flow with account-manager → senior-copywriter → HITL approval gate → client-success packaging. Registered in `builtInTemplates` so the marketing/agency domain pack picks it up by default.
