---
---

Add a pure `cronPreview` helper that parses 5-field cron expressions (wildcards, lists, ranges, steps, `7→0` Sunday normalization) and returns the next N firing times, paving the way for the visual cron UI on background agents.
