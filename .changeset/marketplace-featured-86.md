---
'@agentskit/os-marketplace-sdk': patch
---

#86: add `selectFeatured` (slot-aware ranking for `editors-picks` / `trending` / `new` / `most-installed`), `buildRatingHistogram`, and `buildUsageStats` (7d/30d installs + weekly growth rate). Pure helpers the marketplace front page renders.
