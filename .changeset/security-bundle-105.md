---
'@agentskit/os-core': patch
---

#105: add `createSecurityBundle` — composes prompt firewall (#443/#200) + redaction profile (#439) + sandbox pick (#190) into one object the runtime queries for prompt screening and tool sandbox launch. Pure; every dependency caller-supplied.
