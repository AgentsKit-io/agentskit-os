---
'@agentskit/os-templates': patch
---

#338: add official domain packs (engineering / finance / marketing / support / clinical) bundling starter template ids, eval pack ids, recommended policy profile ids, dashboard widget ids, and tags. `OFFICIAL_DOMAIN_PACKS` + `getOfficialDomainPack` surface them to the CLI's `os new` flow.
